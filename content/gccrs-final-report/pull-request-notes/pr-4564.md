---
title: "Add Drop support for block-local variables"
layout: "pr-detail"
number: "#4564"
status: "Merged"
status_class: "merged"
pr_url: "https://github.com/Rust-GCC/gccrs/pull/4564"
group: "manual-drop"
weight: 20
---

## Objective

Add automatic Drop calls for supported local bindings at block exit and implicit function exit.

## Why it was needed

Registering the Drop trait did not run cleanup by itself.

gccrs still did not call `Drop::drop` when a supported local binding reached the end of its block.

This PR added the first automatic cleanup path for simple local names.

## Main changes

- Adds tracking for block-local Drop candidates.
- Builds a Drop call for each supported candidate when its block ends.
- Reports unsupported Drop `ref` patterns and subpatterns.
- Moves the Drop helpers into separate `rust-compile-drop` files.
- Adds Drop calls at the end of function bodies for implicit returns.

## Test cases

- [drop-block-scope.rs](https://github.com/Rust-GCC/gccrs/blob/9af2a5f747071f9c608e6f89557be875040a91a0/gcc/testsuite/rust/execute/drop-block-scope.rs): Creates `_x`, `x`, and `mut x` in separate blocks and checks that all three Drop calls run.
- [drop-ref-pattern.rs](https://github.com/Rust-GCC/gccrs/blob/9af2a5f747071f9c608e6f89557be875040a91a0/gcc/testsuite/rust/compile/drop-ref-pattern.rs): Checks the unimplemented diagnostic for `let ref _x = Droppable`.
- [drop-function-scope-unit.rs](https://github.com/Rust-GCC/gccrs/blob/9af2a5f747071f9c608e6f89557be875040a91a0/gcc/testsuite/rust/execute/drop-function-scope-unit.rs): Checks that a local in a unit-returning function runs its Drop call.
- [drop-function-scope.rs](https://github.com/Rust-GCC/gccrs/blob/9af2a5f747071f9c608e6f89557be875040a91a0/gcc/testsuite/rust/execute/drop-function-scope.rs): Checks that a local in a function with the tail value `0` runs its Drop call.

> **Scope of this PR**
>
> Supported bindings in this patch use simple names such as `x`, `mut x`, and `_x`.
>
> `ref` bindings and subpatterns are not supported.
